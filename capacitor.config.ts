import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'plugin.test.capacitor.5',
  appName: 'plugin-test-capacitor-5',
  webDir: 'build'
};

export default config;
